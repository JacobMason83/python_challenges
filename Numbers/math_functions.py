import math #math library python is famous for it 

loss = -20.25
product_cost = 89.99
#all the math functions they are lowercase m not uppercase m
# abs absoulute value of makes it positive
print(abs(loss))
# math.floor it rounds it down to the nearest value
print(math.floor(loss))
# Math.ceil is for math ceiling aka rounded up value
print(math.ceil(loss))
#can be combined
print(abs(math.floor(loss)))
#round rounds the number 
print(round(loss))
#math sqrt square root 
print(math.sqrt(loss)
# math power function  also is basically doing print(5**2) but power is so much better
print(math.pow(loss)
